### Fixed

- **`hostwarden-wrap` keeps a Docusaurus admonition's fence on its own
  line.** A closing `:::` right after a long paragraph line was joined
  onto that line, and the admonition then took in the rest of the page.
