### Changed

- **The mirror page says when your git hosting's own pull mirror is
  enough.** A plain copy, such as a public one on Forgejo, needs no
  script; `bin/hostwarden-mirror` is for a mirror production clones
  from. Either way Actions stay off in the mirror, on Forgejo too.
