This directory is a hostwarden workspace: the state of the
servers this install operates. Its presence tells hostwarden
that the checkout around it administers servers rather than
develops hostwarden itself. Created by bin/hostwarden-init;
keep it under version control with the rest.
