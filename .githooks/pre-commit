#!/bin/sh
# pre-commit — keep a secret out of the commit being made. Only the
# staged changes, so it is quick; the full history is scanned
# before every push and in CI.
exec sh "$(git rev-parse --show-toplevel)/scripts/check.sh" --pre-commit
